interface:
  display_name: "Computer Use macOS"
  short_description: "Run the bundled standalone macOS computer-use MCP server"
  default_prompt: "Use the skill-bundled standalone macOS computer-use MCP server, build it if needed, let it self-bootstrap its runtime, and avoid any dependency on a local Claude installation."
