interface:
  display_name: "Computer Use macOS"
  short_description: "Run the extracted standalone macOS computer-use MCP server"
  default_prompt: "Wire up and run the standalone macOS computer-use MCP server from this repository, including env checks and build steps."
